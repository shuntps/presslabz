{
  "name": "@presslabz/tokens",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./breakpoints": "./src/breakpoints.ts",
    "./preferences": "./src/preferences.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "//": "Import ./breakpoints or ./preferences from anything without a DOM — the root entry pulls in document and HTMLElement.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
