{
  "name": "@presslabz/e2e",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "//": "Browser tests. Not part of `pnpm test`: they need Postgres, Valkey, the object store and two servers, which a unit run must not require.",
  "scripts": {
    "e2e": "node scripts/prepare.ts && playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@axe-core/playwright": "catalog:",
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "iovalkey": "catalog:",
    "postgres": "catalog:",
    "typescript": "catalog:"
  }
}
